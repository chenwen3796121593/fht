# 烽火台 TradeBoard — 完整开发记录

> 2026.6.22-27 · 线上 https://fenghuotai.pages.dev · GitHub chenwen3796121593/fht

---

## 一、项目概要

八页跨市场行情看板：首页·自选·指标·新闻·聊天·预警·贵金属·VIP  
React 19 + Vite 8 + Tailwind CSS 4 + Supabase + TradingView Lightweight-Charts v5 + Cloudflare Pages

---

## 二、架构设计

### Context 全局状态
- `AppContext`：路由导航、行情数据(quotes+prices)、Toast通知、涨跌数据(预加载+10s刷新)、自选品种管理
- Toast 直接在 AppProvider 内渲染，Context 驱动，无额外组件

### 懒加载 + 预加载
- Dashboard、IndicatorsPage、ChatPage、VipPage 使用 `React.lazy` 分包
- 首页渲染后 `requestIdleCallback` 后台预加载其余4个页面
- lightweight-charts(168KB) 和 Supabase(201KB) 独立分包，首屏仅222KB

### 数据流
- 本地开发：Vite → server.cjs(Node代理) → 新浪/东方财富/财联社
- 生产环境：Cloudflare Pages → CF Functions → 新浪(HTTPS)/东方财富

---

## 三、各页面详情

### 🏠 首页
- 7品种横滚卡片(MarketBar)，3s轮询
- 两市成交额 + 市场温度计(Snowflake/Flame图标)
- 涨跌家数：财联社 API，10s刷新，localStorage 5分钟缓存秒显（AppContext启动时预加载）
- 板块资金流向 TOP10：东方财富 emdatah5

### 📈 自选
- TradingView lightweight-charts v5 K线图(CandlestickSeries+HistogramSeries)
- 分时/日线/全部三种模式，setData 增量刷新
- 商品日K服务端限300根（避免18年数据撑爆图表），日线显示30根
- 分时K线：intraday转Unix时间戳，日线保持日期字符串
- 自选品种支持中文名搜索、代码添加、localStorage持久化
- 代码前缀自动补全(sh/sz/bj)，统一 `normalizeSymbol` 函数

### 📊 先行指标
- 2×2四宫格 SVG折线图：M1/M2货币供应、新增贷款、贷款增速、准备金率
- 右上轴叠加灰色虚线上证月线
- 东方财富 datacenter API，1h自动刷新，24h localStorage缓存

### 📰 新闻
- 股票/商品RSS(AI精选+星级排序) + 新浪宏观(关键词筛选)
- 宏观5min自动更新，股票商品按需刷新

### 💬 聊天
- Supabase Realtime(Postgres Changes 订阅)
- 48个Lucide图标表情面板(6行×8列，日常/交易/社交分类)
- 文字乐观更新(temp ID)+语音blob预览+后台上传
- localStorage消息缓存(最近30条)，打开秒显
- 消息时间显示：今天只显时分，昨天及更早显示月日+时分

### 🔔 预警
- 涨跌幅/价格突破/价格跌破三种触发条件
- 推送(Notification API)+震动(Vibration API)+语音(SpeechSynthesis)
- 5分钟防骚扰冷却，品种名自动取实时行情全名(从Context prices)

### 💰 贵金属
- **实时行情表**：沪金/沪银/铂金/钯金 销售价(卖价)，新浪期货数据(nf_AU0/AG0/PT0/PD0)，30s轮询+localStorage缓存
- **商品预测表**：Supabase commodity_rankings 实时订阅，品种/现价/7天/14天/30天五列，高于现价红色低于绿色
- flex布局：表格区可滚，底部文字+微信二维码固定不跳
- 白银元/千克→元/克自动转换

### 👑 VIP
- 拆分为 VipHome/VipLogin/VipMember/VipAdmin 四个子组件
- 管理员登录：CF Function JWT校验(环境变量 ADMIN_USER/PASS/SECRET)，本地回退硬编码
- 会员登录：Supabase vip_users 表验证
- 策略发布+申请审核(通过自动创建会员账号/拒绝)
- Toast内联反馈(✅/❌)

---

## 四、优化记录

| 日期 | 优化项 |
|------|------|
| 6.26上午 | Context架构+VIP拆分+lightweight-charts迁移+表情面板+电子时钟+骨架屏+PWA增强+响应式+涨跌缓存 |
| 6.26下午 | 缓存放宽5分钟+语音占位文字+指标lightweight-charts+聊天日期显示+贵金属页(新浪数据源)+二维码 |
| 6.26晚上 | 懒加载分包(222KB)+预加载+缓存策略(_headers)+normalizeSymbol提取+Toast死代码删除+K线setData刷新+JWT安全加固+商品K线限300根+iTick彻底删除+分时时间戳修复+Cloudflare部署清理(187→1) |
| 6.27 | 贵金属页AI商品预测表(Supabase实时订阅)+flex防跳布局+二维码固定+占位行数自适应+VIP文案+@提及功能(Presence+在线/离线列表+高亮+绿点提醒)+视频通话(CF Worker+DO WebSocket信令+腾讯STUN+Metered TURN) |
| 6.28 | 代码审计全修: 视频音频多轨道修复+VIP密码SHA-256哈希+指标页增量setData去闪烁+错误emoji修正+二维码更新+每日聊天缓存清理+聊天输入栏上移 |
| 6.29 | 视频通话全面完成：CF Worker+DO信令+域名fenghuotai.cc.cd+腾讯STUN+布局修复+在线来电(📞+全屏弹窗+铃声振动+接听拒绝)+全局Presence在线追踪+离线推送(已移除)+邀请按钮修复+自动缓存清 |

---

## 五、代码审计与安全加固（6.27）

| 问题 | 严重度 | 解决 |
|------|:--:|------|
| 管理员密码硬编码前端 | 🔴 | 删除 `859168` 回退，强制走 CF Function |
| JWT 密钥 fallback 在源码 | 🔴 | 删除 `fh-secret-change-me`，强制环境变量 |
| 第二个 Supabase 密钥暴露 | 🔴 | 移入 constants.js，建议开启 RLS |
| StockChart 两 effect 竞态 | 🔴 | 合并为一个 effect，chart 创建和数据刷新不冲突 |
| useMarketData 轮询间隔被重置 | 🔴 | useRef 稳定 callback，空依赖数组 |
| Dashboard 符号补全不一致 | 🟡 | 统一调 normalizeSymbol |
| Watchlist 品种列表重复 | 🟡 | 改用 constants.js DEFAULT_WATCHLIST |
| _headers 通配规则冲突 | 🟡 | 精确匹配独立路径 |
| fmtPrice 死代码 | 🟢 | 删除 |
| console.error 残留 | 🟢 | 清理 |
| manifest 描述错误 | 🟢 | 改为"股票+商品+贵金属" |

---

## 六、关键问题与解决

| 问题 | 解决 |
|------|------|
| 网页打开20秒 | index.html 去除 no-cache meta，_headers 设置 1年 immutable |
| 标签切换慢 | React.lazy 懒加载 + requestIdleCallback 预加载 |
| K线全乱 | 商品date字段兼容 + 服务端限300根 + 分时/日线时间格式分流 |
| K线每3秒闪烁 | chart.remove() → series.setData() 增量刷新 |
| 聊天发送不了 | getSB() 动态import + await + 乐观更新tmp_前缀 |
| 语音消息灰色 | blob URL → 占位文字"发送中"，订阅替换 |
| Toast不显示 | 统一走 AppContext Provider 内渲染，删除 Toast.jsx |
| 品种名截断 | normalizeSymbol + 存储名优先 |
| 预警名只有2字 | 从 Context prices 取实时行情全名 |
| 管理员密码泄露 | JWT 回退密钥移除，强制 Cloudflare 环境变量 |
| 首页白屏 | Supabase 动态 import 分包，主包 642KB→222KB |
| Cloudflare 187个部署 | wrangler 循环删除，只保留1个 |
| 贵金属页表格跳动 | flex布局+底部固定+占位行数自适应+flex-shrink-0 |

---

## 六、关键问题与解决（续）

| 问题 | 解决 |
|------|------|
| 视频通话只有画面没声音 | ontrack 改为 map 更新而非 find 跳过，第二路音频不丢 |
| VIP 密码明文存库 | SHA-256 Web Crypto 哈希，登录自动迁移旧明文 |
| 指标页数据刷新闪白屏 | 首建图表 + setData 增量更新，不再销毁重建 |
| Supabase Broadcast 视频信令不通 | 放弃，改用 CF Worker + DO WebSocket 中继 |
| workers.dev 国内被墙 | 待 eu.org 免费域名通过后绑定 |
| 聊天输入栏贴底 | pb-1.5 → pb-3 上移半行 |
| 二维码 CDN 缓存不刷新 | 加 ?v=2 版本参数 |
| 视频邀请消息重复显示 | 取消乐观插入，单走 Supabase 订阅回显 |
| VIP 审批手机号已存在报错 | insert → upsert onConflict username |
| workers.dev 国内被墙 | 注册fenghuotai.cc.cd域名，Worker绑定自定义域名 |
| 视频信令不通 | CF Worker + DO WebSocket中继，腾讯国内STUN |
| 视频ontrack不添加新用户 | map→some+map+spread，新增对端入peers列表 |
| 视频画面布局不对 | grid→flex-1上下均分，对方上自己下 |
| 视频退出残留 | leave信号+200ms延迟关闭WS |
| 多人2×2宫格 | 1人全屏/2人上下/3-4人四宫格自适应 |

## 七、部署

```bash
cd tradeboard
npm run build
npx wrangler pages deploy dist --project-name=fenghuotai --branch=main --commit-dirty=true
```

Cloudflare 环境变量：`ADMIN_USER` / `ADMIN_PASS` / `ADMIN_SECRET`

线上：https://fenghuotai.pages.dev

---

## 八、6.28-30 更新记录

### 🔔 预警页改造 → AI分析 + 预测大模型

#### 标签重构
- 预警标签页拆为三标签：**预测大模型** / **AI分析** / **预警**
- 顶部导航图标从 Bell 改为 Zap（闪电）
- 全局颜色统一为蓝(#3B82F6) + 红(#EF4444) + 绿(#22C55E)，清除琥珀色和紫色

#### AI 深度分析
- 前端输入框 + 流式逐字渲染
- 后端 CF Function 调 DeepSeek API（deepseek-chat 模型）
- System Prompt 采用 Serenity 供应链瓶颈研究框架
- 输入股票代码自动注入实时行情
- VIP 密码校验：前端 SHA-256 哈希 + 后端 CF 环境变量比对
- 密码：SHA-256 存于 AI_ANALYSIS_PASS 环境变量

#### 预测大模型
- 4个模型 × 5份排名表格：
  - Chronos-2：6品种商品 7d/14d/30d
  - Moirai-2：6品种商品 7d/14d/30d/60d/90d（转置表格）
  - Kronos-sm：999精选A股 Top50
  - TimesFM：精选/全量 A股 30d/60d/128d
- 数据源通过 CF Function 代理（GH_DATA_URL 环境变量），前端和公开仓库不暴露
- 目标价颜色：涨红跌绿

#### 密码机制
- AI分析和预测大模型共享密码
- 前端 SHA-256 哈希校验（不调后端）
- 不存 localStorage，每次切标签/刷新都需重新输入
- 输错有红色错误提示

### 🏠 首页优化
- 板块资金数据源从 emdatah5 切换到 push2.eastmoney.com（行业板块数据）
- Service Worker 拦截 /api/flow 代理到 push2（绕过 CF 网络限制）
- 两市成交额新增环比对比：收盘后显示"较昨日 +X亿/-X亿"
- 昨日数据通过 Service Worker 代理 push2his.eastmoney.com 获取

### 📰 新闻页
- 去掉来源和分类显示，只保留时间

### 🔒 安全加固
- GitHub Token 从 git remote 清除
- 数据源地址移至 Cloudflare 环境变量
- VIP 功能密码 SHA-256 前端校验

### 新增环境变量
| 变量 | 用途 |
|------|------|
| DEEPSEEK_API_KEY | AI 分析 DeepSeek API |
| AI_ANALYSIS_PASS | VIP 密码 SHA-256 哈希 |
| GH_DATA_URL | 预测数据 GitHub Raw URL |

### 🖥 HF Space 嵌入
- 预测大模型标签页新增 TimesFM+Moirai / Chronos+Kronos 两个按钮
- iframe 嵌入 Hugging Face Space，不离开页面
- URL 通过 CF Function 代理获取，前端不暴露

### 🔒 安全加固（续）
- 密码哈希从前端移除，改为后端 API 校验（/api/verify-pwd）
- HF Space 地址移至环境变量（HF_TIMESFM_MOIRAI / HF_CHRONOS_KRONOS）
- 新增环境变量：
  - HF_TIMESFM_MOIRAI
  - HF_CHRONOS_KRONOS

### 🐛 Bug 修复
- 贵金属实时行情价格涨跌颜色：从蓝色改回涨红跌绿
- 贵金属昨收价数据源修正：Sina nf_连续合约 parts[3]=昨收
- 首页成交额环比时间修复：盘中隐藏，收盘后~次日开盘前显示

---

## 九、7.1-2 更新记录

### 📰 新闻页重构
- 新增「大佬观点」标签，拆为两个子标签：
  - **国际大佬**：Google News RSS 聚合 Nicky Shiels + Alasdair Macleod（90天），DeepSeek 英译中
  - **券商首席**：Google News RSS 聚合7位首席分析师（30天）
- 数据链路：CF Function → Deno Deploy（跳板）→ Google News
- Deno 代理代码部署在 deno.net Playground
- 国际新闻支持 DeepSeek 翻译按钮，点击翻译原文

### 📊 宏观新闻优化
- 关键词精简为国家级政策机构（央行/证监会/财政部等13个）
- 缓存从5分钟延长至24小时

### 🔧 技术优化
- CF 缓存策略统一
- 贵金属页商品预测改为 Moirai-2（GitHub每日更新），替代废弃的Supabase

### 🌐 新增依赖
- Deno Deploy：Google News RSS 代理
- CF Function `/api/translate`：DeepSeek 翻译

### 🔀 标签页合并
- **首页 + 指标** → 主页（情绪/指标 子标签）
- **贵金属 + VIP** → 大宗商品（贵金属/VIP 子标签，Coins 图标）
- TopBar 从 8 标签精简为 6 标签

### 📰 新闻页优化
- 新增 KITCO 标签（国际贵金属新闻，DeepSeek 翻译）
- 标签排序：股票 | 商品 | KITCO | 宏观 | 大佬观点

### 📊 LightGBM 全量A股
- 新增子标签，数据源：khquant/latest.json（Deno 代理）
- 显示 Top 30：排名/代码/名称/行业/总分
- 模型：Medallion+BlackRock
- 表格下方显示因子覆盖报告（数据时效性）
- NaN 值自动替换为 null（CF 代理清洗）

## 十、7.6 UI全面优化（自适应+下拉菜单+骨架屏）

### 🎨 响应式布局
- 手机保留顶部导航，平板/电脑改为左侧侧边栏（Layout组件）
- 侧边栏：图标+文字标签，宽度80→96px
- TopBar：手机图标下加文字标签，桌面自动隐藏
- 清除所有固定 `h-dvh`/`calc(100vh)` 高度
- 自选列表、新闻内容、Chat页全部响应式适配

### 🔽 子标签下拉菜单
- 三级嵌套子标签统一改为下拉式（TabDropdown组件）
- 预测大模型：模型标签 + HF链接 分两个下拉
- 大佬观点：国际大佬/券商首席 → 新闻栏独立下拉按钮

### ✨ UI细节
- 新闻加载态 → 骨架屏动画
- 主页情绪/指标加图标
- 表格 `overflow-y-auto` 可滚动
- 新闻标签栏 z-index 遮挡修复

### 📊 成交额卡片优化
- 新增较前日对比（SW lmt=3→4，API 增加 prevTotal）
- 两行紧凑排版：成交额 + 较昨日/较前日并排

### 🎛 大模型页 UI 细节
- 「预测大模型」→「大模型」
- 三个主标签 flex-1 等宽
- 模型+HF 分两个下拉，统一最小宽度
- 刷新按钮移至表格日期旁（小图标）
- 下拉按钮非选中态改灰色（去掉默认蓝色fallback）
- 所有模型表格加日期+刷新头栏
